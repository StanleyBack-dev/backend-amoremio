# As migrations herdadas do projeto anterior foram removidas.
# Gere um baseline novo com `npm run migration:generate:dev` (a partir das
# entidades finais) antes de qualquer deploy fora de desenvolvimento.
# Em desenvolvimento o schema é criado por `synchronize: true`.
