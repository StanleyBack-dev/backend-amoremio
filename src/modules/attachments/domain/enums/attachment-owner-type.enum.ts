// Which aggregate an attachment belongs to. Each value needs an
// AttachmentOwnerPolicy registered by the module that owns that aggregate.
export enum AttachmentOwnerType {
  PRODUCT = "PRODUCT",
}
